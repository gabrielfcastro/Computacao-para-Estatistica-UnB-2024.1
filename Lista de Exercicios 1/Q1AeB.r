# Construindo um vetor com os 8 primeiros dígitos matrícula (incluindo o ano).
# vet = c(2,0,2,0,6,6,5,7,"FIM")
vet = c(2,0,2,0,6,6,5,7)
vet

# Agora vamos construir uma matriz com 2 linhas e 4 colunas com os dois digitos do ano sendo a primeira coluna da minha matriz
matrix(vet, nrow = 2, ncol = 4)

